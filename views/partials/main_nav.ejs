<!-- Main nav -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">COMP 229</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        
        <% if (title == "Home") { %>
          <li class="nav-item active">
          <% } else { %>
            <li class="nav-item active">
          <% } %>
          <a class="nav-link" href="/home"><i class="fa-solid fa-house"></i> Home</a>
        </li>

        <% if (title == "About") { %>
          <li class="nav-item active">
          <% } else { %>
            <li class="nav-item active">
          <% } %>
          <a class="nav-link" href="/about"><i class="fa-solid fa-question"></i> About</a>
        </li>

        <% if (title == "Projects") { %>
          <li class="nav-item active">
          <% } else { %>
            <li class="nav-item active">
          <% } %>
          <a class="nav-link" href="/projects"><i class="fa-solid fa-paperclip"></i> Projects</a>
        </li>

        <% if (title == "Services") { %>
          <li class="nav-item active">
          <% } else { %>
            <li class="nav-item active">
          <% } %>
          <a class="nav-link" href="/services"><i class="fa-solid fa-clipboard"></i> Services</a>
        </li>

        <% if (title == "Contact") { %>
          <li class="nav-item active">
          <% } else { %>
            <li class="nav-item active">
          <% } %>
          <a class="nav-link" href="/contact"><i class="fa-solid fa-comments"></i> Contact</a>
        </li>
       
    </div>
  </nav>
